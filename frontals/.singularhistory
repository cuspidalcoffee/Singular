LIB "presmatrix.lib";
ring r=0,(x,y,z),ds;
ring s=0,(X,Y,Z,T),ds;
setring r;
map f=s,x, y, 2*y*z + 3*x*z^2 + 5*z^4, -(y*z^2) - 2*x*z^3 - 4*z^5;
ls
;
ideal i=0;
presmatrix(f,i);
setring s;
ideal d3=fitting(PM,2);
d3=std(d3);
d3;
print(d3);
ideal d2=std(fitting(PM,1));
d2;
print(d2);
setring r;
map f=s,x, 2*x*y + 3*y^2 + z^2, 2*y*z, -(x*y^2) - 2*y^3 - 2*y*z^2;
presmatrix(f,i);
setring s;
ideal d2=std(fitting(f,1));
ideal d2=std(fitting(PM,1));
ideal d3=std(fitting(PM,2));
print d2;
print(d2);
print(d3);
setring r;
map f=s,x, 2*x*y + 3*y^2 - z^2, -2*y*z, -(x*y^2) - 2*y^3 + 2*y*z^2;
presmatrix(f,i);
setring s;
ideal d2=std(fitting(PM,1));
ideal d3=std(fitting(PM,2));
print(d2);
print(d3);
quit;
